# Cluster Configuration (Dev)

This folder contains GitOps configuration files for the development
Kubernetes cluster used in the FluxPilot project.

## Purpose
The manifests in this folder define how Flux (GitOps tool) connects
to the cluster and synchronizes application state from this repository.

## Notes
- This setup represents a development environment.
- Similar folders can be created for staging or production clusters.
